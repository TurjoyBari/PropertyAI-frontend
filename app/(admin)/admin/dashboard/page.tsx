import { DashboardView } from "@/components/dashboard/dashboard-view";

/** Admin home dashboard — KPIs, charts, and activity (full catalog scope). */
export default function AdminDashboardPage() {
  return <DashboardView />;
}
