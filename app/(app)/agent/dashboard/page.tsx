import { DashboardView } from "@/components/dashboard/dashboard-view";

/** Agent home dashboard — scoped to this agent's inventory/leads. */
export default function AgentDashboardPage() {
  return <DashboardView />;
}
